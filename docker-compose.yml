version: "3.9"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: testops-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
